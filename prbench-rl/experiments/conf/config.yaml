# Default configuration for RL experiments
defaults:
  - agent: sac_humanoid_v4
  - _self_

# Experiment mode: "train" or "eval"
mode: train
env_id: Humanoid-v4
max_episode_steps: 300
load_agent_path: null
eval_episodes: 10

# Random seed
seed: 0