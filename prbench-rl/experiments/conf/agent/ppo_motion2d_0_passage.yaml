# PPO agent configuration
name: ppo

# Core training parameters
args:
  total_timesteps: 1000000
  num_eval_envs: 1
  # Network architecture
  hidden_size: 64

# Training settings
torch_deterministic: true
cuda: false

# Logging settings
exp_name: "ppo_m2d_0_passage_train_1123_1m"
tb_log_dir: "runs"