# PPO agent configuration
name: sac

# Core training parameters
args:
  total_timesteps: 1000000
  num_eval_envs: 1
  hidden_size: 64
  
# Training settings
torch_deterministic: true
cuda: false

# Logging settings
exp_name: "sac_m2d_1_button_train_1119_1m"
tb_log_dir: "runs"